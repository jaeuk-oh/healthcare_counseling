# 보건소 의료비 지원 상담 AI

> 하남시 보건소 의료비 지원 전화 상담원을 위한 AI 1차 응대 시스템

시민이 전화로 상황을 설명하면, AI가 해당 가능한 지원 정책(암의료비지원 / 희귀질환의료비지원 /
국가암검진)을 분류하고 체크리스트로 자격 요건을 하나씩 확인합니다. 소득·재산처럼
전화로는 판정할 수 없는 항목은 **판정하지 않고 내방 안내로 마무리**합니다.

이 프로젝트의 핵심은 "LLM이 상담을 잘하게 만드는 것"이 아니라, **업무 규정 중 무엇을
LLM에게 해석하게 하고 무엇을 시스템이 강제해야 하는지 그 경계를 정의하는 것**입니다.

## 데모

| | |
|---|---|
| **1. 초기 화면** — 좌측 채팅, 우측 체크리스트 카드로 화면이 분리돼 있습니다. | **2. 정책 분류** — "위암 진단 + 건강보험 가입자"라는 발화만으로 성인암의료비지원 정책이 잡히고, 우측에 체크리스트가 생성됩니다. |
| ![초기 화면](docs/images/demo-01-empty.jpg) | ![정책 분류](docs/images/demo-02-classify.jpg) |
| **3. 체크리스트 갱신** — 수검일·진단일을 답하자 해당 항목이 초록으로 바뀝니다. 건강보험료(소득) 항목은 전화로 판정하지 않는 항목이라 계속 미확인(회색)으로 남습니다. | **4. 내방 안내로 마무리** — 확정할 수 없는 항목이 남으면 얼버무리지 않고 주소·운영시간·지참서류까지 구체적으로 안내하며 종료합니다. |
| ![체크리스트 갱신](docs/images/demo-03-checklist.jpg) | ![내방 안내](docs/images/demo-04-handoff.jpg) |

> 이 캡처를 뜨는 과정에서 "월 소득 300만원이면 지원이 어려울 것 같다"고 LLM이
> 자연어로 먼저 판단해버리는 턴을 실제로 관측했습니다. 코드 가드레일은 체크리스트의
> `건강보험료 기준` 항목을 미확인 상태로 정확히 고정시켰지만, 자유 응답 텍스트 자체가
> 소득을 판정해버리는 것은 막지 못한 것입니다. 이건 이 프로젝트가 아직 못 막은 문제라
> [한계](#한계)에 그대로 추가했습니다 — 데모를 좋게 보이려고 감추지 않았습니다.

실제 동작 로그(텍스트) 재현은 [DESIGN.md](DESIGN.md#실제-동작-로그-재현)에서도 볼 수 있습니다.

## 목차

- [기능](#기능)
- [데모](#데모)
- [빠른 시작](#빠른-시작)
- [핵심 설계](#핵심-설계)
- [검증 결과](#검증-결과)
- [한계](#한계)
- [기술 스택](#기술-스택)
- [더 읽어보기](#더-읽어보기)

## 해결하는 문제

대상 사용자는 시민이 아니라 하남시 보건소 의료비 지원 전화 상담을 받는 **상담원**입니다.
상담원은 한 통화 안에서 시민의 상황(진단명, 보험 유형, 진단·검진 시점 등)을 파악하면서
동시에 정책별 소득기준·대상질환·신청조건·예외조건을 대조해야 하는데, 이 대조에서 실수가
나면 두 방향으로 피해가 생깁니다.

- **오안내**: 자격이 안 되는데 "될 것 같다"고 하면 시민이 서류를 들고 헛걸음합니다.
- **판정 불가 항목의 임의 확정**: 소득·재산은 서류로만 확인 가능한데, LLM은 대화
  맥락만으로 "기준을 충족하신 것 같다"고 단정해버리기 쉽습니다.

필자는 하남시 보건소 만성질환팀 상담원으로 근무한 경험이 있고, 이 두 실패 유형은 실제
업무에서 반복적으로 관측한 것입니다 ([DOMAIN_NOTES.md](DOMAIN_NOTES.md)에 구체 사례 5건 기록).
왜 이 문제가 "자동화"가 아니라 "판단 경계를 정하는 문제"였는지는 [DESIGN.md](DESIGN.md)에서 다룹니다.

## 기능

- **정책 분류**: 시민 발화에서 해당 가능한 지원 정책을 GPT function calling으로 자동 선별
- **체크리스트 순차 확인**: 정책별 자격 조건을 대화 턴마다 상태 머신으로 갱신
- **전화 판정 불가 항목 동결**: 소득·재산처럼 서류로만 확인 가능한 항목은 LLM 출력과
  무관하게 항상 미확인(`None`) 처리
- **내방 핸드오프**: 확정할 수 없는 항목이 남으면 상담을 얼버무리지 않고 내방 안내로 전환

## 빠른 시작

### 요구사항

- Python 3.12, Node 18+
- OpenAI API 키 (필수). Supabase는 선택 — 비워두면 세션 저장을 건너뜁니다.

### 백엔드

```bash
python -m venv .health_venv
source .health_venv/bin/activate
pip install -r requirements.txt
```

`.env`에 아래 값을 채웁니다. `OPENAI_API_KEY`만 필수이고 나머지는 기본값이 있습니다.

```bash
OPENAI_API_KEY=          # 필수
CHROMA_PERSIST_DIR=      # 선택, 기본값 ./chroma_db
LLM_MODEL=               # 선택, 기본값 gpt-4o
SUPABASE_URL=            # 선택 — 비우면 세션 저장을 건너뜀
SUPABASE_ANON_KEY=       # 선택
```

```bash
python scripts/ingest_policies.py   # 정책 문서 3종을 ChromaDB에 인덱싱 (최초 1회)
uvicorn src.api:app --reload --port 8000
```

### 바로 확인해보기

```bash
# 단일턴 정책 분류
curl -s -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"query": "위암 진단을 받았는데 건강보험 가입자예요. 지원받을 수 있나요?"}'

# 멀티턴 상담 — 첫 턴은 checklist를 빈 배열로 보냄
curl -s -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
        "messages": [
          {"role": "user", "content": "위암 진단을 받았는데 건강보험 가입자예요. 지원받을 수 있나요?"}
        ],
        "checklist": []
      }'
```

`/chat` 응답의 `checklist`를 다음 요청의 `checklist`에, 누적된 `messages`와 함께
그대로 실어 보내면 상담이 이어집니다. 서버는 세션 상태를 저장하지 않고 클라이언트가
매 턴 전체 상태를 들고 다닙니다 ([DESIGN.md §3.1](DESIGN.md#31-아키텍처--rag--체크리스트-상태-머신)).

### 프론트엔드

```bash
cd frontend
npm install
echo "NEXT_PUBLIC_API_URL=http://localhost:8000" > .env.local
npm run dev
```

### 주요 엔드포인트

| 경로 | 역할 |
|---|---|
| `POST /classify` | 발화 → 해당 가능 정책 분류 |
| `POST /chat` | 멀티턴 상담. `messages`·`checklist`를 매 턴 클라이언트가 보유해 전달 |
| `POST /query` | 단일턴 정책 분류 (레거시 경로, eval에서 사용) |
| `POST /session-end` | 상담 세션 저장 (Supabase 미설정 시 생략) |
| `GET /health` | 헬스체크 |

## 핵심 설계

"무슨 기술을 연결했는가"보다 "누가 무엇을 책임지는가"가 이 설계의 핵심입니다.

| 레이어 | 맡는 것 | 왜 여기인가 |
|---|---|---|
| **LLM** | 자연어 이해, 정책 분류, 상담 진행, 체크리스트 항목 추출·업데이트 | 시민 발화 형식이 정해져 있지 않아 규칙으로 다 열거할 수 없는 유연한 판단이 필요 |
| **코드 / Rule** | 전화 판정 불가 항목 동결, 결정적 불가 조건 우선 적용, 확정값 보호, 출력 스키마 검증 | 틀리면 피해가 직접적이고 되돌릴 수 없는 지점 — 프롬프트 지시만으로는 신뢰할 수 없어 코드로 강제 |
| **사람 (상담원 / 담당자)** | 내방 안내 이후 서류 확인, 최종 자격 판정 | 소득·재산처럼 시스템이 원천적으로 확인할 수 없는 판단 |

판단은 LLM이 하지만, 아래 네 가지는 LLM 출력과 무관하게 코드가 항상 강제합니다.

1. **전화 판정 불가 항목 동결** — 소득·재산 등 `visit` 항목은 LLM이 뭐라고 출력하든 미확인으로 고정
2. **결정적 불가 조건을 LLM 판단보다 우선** — cutoff 조건이 검출되면 LLM의 최종 판단을 교정
3. **확정값 보호** — 확정된 값이 이후 턴에서 미확인 상태로 되돌아가지 못하게 차단
4. **LLM 출력 구조 검증** — 스키마 위반 응답은 조용히 버림 (오염보다 누락이 안전)

각 가드레일이 어떤 실패를 막기 위한 것인지, RAG를 의도적으로 얕게 쓴 이유, 아키텍처
전체 흐름은 [DESIGN.md](DESIGN.md)에 정리했습니다.

## 검증 결과

**LLM이 개입하는 층은 실행마다 결과가 달라지므로 여러 번 돌린 분포를 기록하고,
코드로 강제하는 층은 LLM 없이 결정적으로 검증합니다.**

- 코드 가드레일: **15/15** (결정적 — `tests/test_guardrails.py`, LLM·서버 불필요)
- `/query` 정책 분류: 5회 실행에서 **7~10/10**
- `/chat` 멀티턴 상담: 3회 실행에서 **4~6/6**

> **단일 실행 결과를 성적표로 쓰면 안 된다는 것이 이 프로젝트에서 얻은 교훈 중 하나입니다.**
> 시나리오별 상세 결과, 실패 유형(미종료·체크리스트 업데이트 누락), 불안정한 케이스가
> 왜 불안정한지는 [EVAL.md](EVAL.md)에 정리했습니다.

```bash
python tests/test_guardrails.py      # 결정적, LLM·서버 불필요
python tests/run_eval.py             # /query 10 시나리오, 서버 실행 필요
python tests/run_counselor_eval.py   # /chat 6 시나리오, LLM 시민 시뮬레이터 포함
```

## 한계

솔직하게 기록합니다.

**현재 한계**
- 정책 커버리지는 암의료비지원(소아암 포함)·희귀질환의료비지원·국가암검진 3종으로 한정.
  산정특례·재난적의료비는 미포함.
- "담당자 핸드오프"의 실체는 에스컬레이션 큐나 알림이 아니라, 상담 세션을
  Supabase에 저장해 담당자가 이후 참조할 수 있게 남기는 수준입니다.
- 소득 기준 금액이 정책 문서 frontmatter에 구조화돼 있으나, 현재는
  LLM 컨텍스트로만 쓰이고 비교·계산 로직에는 쓰이지 않습니다.
  (전화 상담에서는 소득을 판정하지 않는다는 설계 원칙에 따른 의도적 선택)

**측정으로 드러난 한계** (상세는 [EVAL.md](EVAL.md))
- `/chat` 멀티턴 결과가 실행마다 4~6/6으로 흔들립니다. 미종료(10턴 소진)와 체크리스트
  업데이트 누락이 주 원인이고, 재현 조건을 아직 특정하지 못했습니다.
- 건강보험 가입자 삼분법 판정이 실행마다 뒤집힙니다. 조건이 정책 문서 텍스트로만
  있고 코드 규칙이 아니기 때문입니다.
- eval 하네스 자체의 한계: 종료 판정이 키워드 매칭(`TERMINATION_SIGNALS`)이라
  표현이 달라지면 오판할 수 있습니다.
- **자연어 응답이 소득을 판정해버리는 경우가 있습니다.** 코드 가드레일은 체크리스트의
  `visit` 항목(건강보험료 등)을 미확인 상태로 정확히 동결시키지만, 이는 구조화된
  상태값만 보호합니다. 데모 캡처 중 "월 소득 300만원이면 지원이 어려울 것 같다"고
  LLM이 자유 응답 텍스트로 먼저 단정해버리는 턴을 실제로 관측했습니다 — 이 프로젝트가
  막으려던 "판정 불가 항목의 임의 확정" 실패 유형이 텍스트 레이어에서는 여전히
  가능하다는 뜻입니다.

**다음 단계**
- ~~평가 시나리오 확장 및 측정 결과 README 반영~~ ✅ 완료
- ~~`/query` eval S09 — 국가암검진 안내 문의를 지원 신청과 구분~~ ✅ 완료 (5/5 통과)
- ~~LLM 스키마 위반으로 인한 500 크래시 방어~~ ✅ 완료 (`/query`·`/chat` 양쪽)
- **삼분법 조건을 규칙 데이터로 분리** — 수검일·진단일 cutoff를 문서 텍스트가 아니라
  날짜 비교 로직으로 옮겨 비결정성을 제거. 지금 가장 우선순위가 높은 항목입니다.
- **자유 응답 텍스트의 소득 판정 억제** — 체크리스트 상태값은 코드로 동결되지만
  자연어 응답이 소득을 임의 판정하는 것은 아직 막지 못합니다. 응답 생성 프롬프트에
  `visit` 항목 관련 명시적 판정 금지 지시를 추가하거나, 응답 텍스트에 대한
  사후 검증(post-hoc check)이 필요합니다.
- **멀티턴 미종료 대응** — 체크리스트가 모두 채워졌는데도 종료 안내로 수렴하지 못하는
  경로를 코드에서 감지해 마무리를 강제
- 체크리스트 업데이트 누락 감지 — 시민 발화에 정보가 있는데 항목이 `None`으로 남는 경우
- 구조화된 부서 라우팅(referral)을 `/chat` 경로와 UI에 연결
- 건강보험공단 API 연동(보험료 자동 조회) — 지금은 전화로 자기신고에 의존
- ~~실제 사용 화면 스크린샷/GIF 촬영~~ ✅ 완료 ([데모](#데모) 섹션)

## 기술 스택

- **백엔드**: FastAPI, OpenAI SDK(GPT-4o, text-embedding-3-small), ChromaDB
- **프론트엔드**: Next.js 15 (채팅 패널 + 정책 체크리스트 카드 UI)
- **저장**: Supabase (세션·메시지·토큰 사용량 저장, 미설정 시 graceful skip)

**의도적으로 쓰지 않은 것들**:

- **LangChain** — 임베딩 → 검색 → LLM으로 이어지는 직선 파이프라인에 추상화
  레이어를 얹을 이유가 없었습니다. OpenAI SDK를 직접 호출하는 편이 어떤 문서가
  검색됐고 LLM에 무엇이 전달됐는지 디버깅하기 더 투명합니다.
- **정책별 전담 에이전트 (멀티 에이전트)** — 한 시민이 복수 정책에 동시에
  해당하는 경우가 많아서, 에이전트별 결과를 합산·조정하는 별도 조정자가
  결국 단일 LLM 호출과 같은 역할을 하게 됩니다. 조정자 하나를 더 만드는
  비용만 늘어나는 구조라 단일 RAG 파이프라인을 선택했습니다.
- **Playwright 크롤링** — 대상 공공기관 페이지가 정적 렌더링임을 먼저 확인하고
  BeautifulSoup으로 대체했습니다. 브라우저를 띄울 이유가 없는 곳에 띄우는 것은
  불필요한 복잡도입니다.

## 더 읽어보기

- [DESIGN.md](DESIGN.md) — 문제 발견 과정, 역할 분리, 가드레일 4가지 상세, RAG를 얕게 쓴 이유, 실제 동작 로그
- [EVAL.md](EVAL.md) — 시나리오별 eval 결과와 실패 유형 상세
- [DOMAIN_NOTES.md](DOMAIN_NOTES.md) — 현업 상담원 경험에서 나온 암묵지 반영 이력 5건
- [NEXT_PROJECT.md](NEXT_PROJECT.md) — 이 프로젝트에서 발견한 문제를 일반화한 다음 프로젝트(B2B 상담 검증 Copilot) 계획

공공기관 공개 자료를 크롤링한 데이터를 포함하고 있어 라이선스는 아직 정하지 않았습니다.
